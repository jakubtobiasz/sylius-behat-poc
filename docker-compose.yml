services:
  mariadb:
    image: mariadb:11
    ports:
      - '3306:3306'
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: sylius_behat_test
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data:
